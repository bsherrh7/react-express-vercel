{
  "name": "react-express",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@babel/cli": "^7.17.10",
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "directory-tree": "^3.3.0",
    "ejs": "^3.1.8",
    "esbuild": "^0.14.43",
    "express": "^4.18.1",
    "handlebars": "^4.7.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "vercel-build": "esbuild src/index.js --bundle --loader:.js=jsx --outfile=build/app.js && esbuild server/server.js --bundle --loader:.js=jsx --outfile=api/server.js --platform=node && cp src/App.js build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
